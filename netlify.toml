[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.18.1"
  NPM_VERSION = "10.8.2"
  NPM_FLAGS = "--include=dev --legacy-peer-deps --no-audit --no-fund"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."supabase-pulse"]
  schedule = "0 7 */5 * *"
